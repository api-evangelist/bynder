openapi: 3.2.0
info:
  title: Bynder Automation Workflow Actions API
  version: 1.0.0
  description: 'Operations tagged Automation Workflow Actions across 2 of this provider''s published API definitions: bynder-automation-workflow-openapi.json, bynder-automation-workflow-svc-openapi.json. Each path carries the servers of the definition it was published in.'
servers:
- url: https://example.com/api/1/automation-workflow/
tags:
- name: Automation Workflow Actions
paths:
  /actions:
    get:
      tags:
      - Automation Workflow Actions
      summary: List Automation Workflow Actions
      description: Get a list of available automation workflow actions.
      operationId: getActions
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutomationWorkflowActionsResponse'
        '400':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorAuthorizationResponse'
      security:
      - {}
      - httpAuth: []
components:
  schemas:
    AutomationWorkflowActionsResponse:
      title: AutomationWorkflowActionsResponse
      type: object
      properties:
        actions:
          type: array
          items:
            $ref: '#/components/schemas/AutomationWorkflowAction'
    AutomationWorkflowAction:
      title: AutomationWorkflowAction
      type: object
      required:
      - action_type
      properties:
        action_type:
          type: string
    ErrorResponse:
      title: ErrorResponse
      type: object
      required:
      - message
      properties:
        message:
          type: string
    ErrorAuthorizationResponse:
      title: ErrorAuthorizationResponse
      type: object
      required:
      - message
      properties:
        message:
          type: string
    AutomationWorkflowActionsResponse_2:
      title: AutomationWorkflowActionsResponse
      type: object
      required:
      - max_actions_limit
      properties:
        actions:
          type: array
          items:
            $ref: '#/components/schemas/AutomationWorkflowAction'
        max_actions_limit:
          type: integer
          format: int32
  securitySchemes:
    httpAuth:
      type: http
      scheme: bearer
x-refined-from:
- bynder-automation-workflow-openapi.json
- bynder-automation-workflow-svc-openapi.json
